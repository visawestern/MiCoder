# Ошибка 05: Effort visibility

**User story:** As a web-provider user, I can see whether effort/thinking was
detected, which levels exist, and whether a selector is available.

| Проверка | Expected | Current quality |
|---|---|---|
| Card status | `Effort: not detected` before discovery | FIXED |
| Detected levels | Shows all persisted levels | FIXED |
| Selector state | Shows selector available when configured | FIXED |
| Refresh action | Runs effort discovery | PASS by code, live QA |
| Send injection | Applies selected effort | PASS by code, live QA |

```text
/goal go over every single feature in this file create a user story with expected behaviour based on the code keep a single canonical spreadsheet tracking the features status
• when done switch loop to testing every user story and documenting all errors
• when done fix every logistical error or ux error
• test every user behaviour again post fix
```
