# Live QA: Kimi / ChatGPT / Qwen

Дата: 2026-08-11. Проверка выполнена через macOS Accessibility API по реальным
окнам MiCoder и WKWebView, без ожидания скриншотов между действиями.

Дополнительно собран новый root bundle через `./build-app.sh --no-bump --skip-tests`:
`MiCoder.app` v2.117 build 115, с `MiCoder_MiCoder.bundle` и `SQLite.swift_SQLite.bundle`.

## Kimi

| Шаг | Результат | Качество |
|---|---|---|
| Provider menu | `🌐 Kimi` доступен | PASS |
| Model menu open | Открыт реальный menu | PASS |
| Models | `K3`, `K3 Swarm` | PASS |
| Model selection | Menu item selectable | PASS |
| Effort refresh | Кнопка `brain.head.profile` доступна | PARTIAL, result not exposed in AX |
| Effort DOM | Реально открыты `Быстрый`, `Стандартный`, `Высокий` в блоке `Интенсивность рассуждений` | FIXED/parser verified |
| Send | Не выполнялся без отдельного disposable prompt/network result | PARTIAL |

## Qwen

| Шаг | Результат | Качество |
|---|---|---|
| Provider menu | `🌐 Qwen` доступен | PASS |
| Refresh models | Нажата реальная кнопка refresh | PASS |
| Model menu before refresh | Был только `Qwen3.7-Max` | EXPECTED pre-refresh |
| Model menu after refresh | `Qwen3.7-Max`, `Qwen3.7-Plus`, `Qwen3.8-Max` | PASS |
| Effort refresh | Кнопка `brain.head.profile` доступна | PARTIAL, result not exposed in AX |
| Send | Не выполнялся без отдельного disposable prompt/network result | PARTIAL |

## ChatGPT

| Шаг | Результат | Качество |
|---|---|---|
| Login sheet | Реальное окно ChatGPT открылось | PASS |
| Cookies | UI показал `94 cookies` | PASS |
| Web page | Реальный ChatGPT composer/buttons обнаружены | PASS |
| Model discovery | Нажат `Find models` на старом installed binary; результат не появился за 10 s | FAIL/PARTIAL |
| Feature filtering | Source parser excludes Research/Image/Canvas/Search/Agent entries | PASS by code, not live result |
| Effort discovery | Selector exists in source/catalog; live result not observed | PARTIAL |
| Send | Не выполнялся, так как live model result не был подтвержден | BLOCKED |

## Important binary distinction

В системе одновременно существовали:

- актуальный `.build/debug/MiCoder` без старой кнопки `Find models`;
- старый `MiCoder.app` с сохранёнными provider cookies и старой UI-кнопкой.

Qwen/Kimi live results получены из сохранённого authenticated installed app.
Для полной ChatGPT live-проверки нужен запуск актуального bundle с теми же
`com.micoder.app` preferences/cookies, иначе свежий SwiftPM executable видит
другое состояние providers.

## Causal verdict

- Qwen: `refresh click -> WebModelDiscovery -> merged selectors -> 3 models -> menu` — PASS.
- Kimi: `provider menu -> model menu -> 2 models` — PASS.
- Kimi effort: `reasoning intensity popup -> Быстрый/Стандартный/Высокий -> low/medium/high` — PASS after parser fix.
- ChatGPT: `login -> cookies -> page -> discovery` — разрыв между page и discovery, PARTIAL.

После live-наблюдения добавлен source-level fallback для поиска видимой кнопки
`ChatGPT`, когда catalog test-id устарел. Targeted и полный XCTest после этого
проходят, но внешний ChatGPT discovery должен быть повторен в одном foreground
bundle с теми же cookies; текущий временный bundle не получил окно при duplicate
bundle identifier и поэтому не дает честного PASS.

После этого исправлен discovery/capture flow: discovery запускается автоматически
и доступен отдельной активной кнопкой, но `Capture session` остается disabled,
пока discovery не нашел модель. Для ChatGPT discovery ограничивается одной
текущей моделью, а не 29 option nodes. Полный suite после исправления:
**1855/1855 теста, 265 suite**. Live ChatGPT result все еще требует foreground
окна свежего bundle с теми же cookies.

```text
/goal go over every single feature in this file create a user story with expected behaviour based on the code keep a single canonical spreadsheet tracking the features status
• when done switch loop to testing every user story and documenting all errors
• when done fix every logistical error or ux error
• test every user behaviour again post fix
```
